<div class="row mt-3">
  <div class="col-12">
    <div class="card">
      <div class="card-header">
        <%= button_to "登出系统", session_path(@user), method: :delete, data: { turbo_confirm: "确定登出系统？" }, class: "btn btn-sm btn-danger float-end" %>
      </div>
      <div class="card-body">
        <h6 class="card-title"><%= @user.email_address%> 的工作台</h6>
      </div>
    </div>
  </div>
</div>
