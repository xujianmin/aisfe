<% if index.even? %>
  <div class="card mb-3">
<% else %>
  <div class="card mb-3 bg-light-subtle">
<% end %>
  <div class="card-header"><%= visit.created_at.strftime("%Y-%m-%d %H:%M") %></div>
  <div class="card-body">
    <%= visit.content %>
  </div>
</div>