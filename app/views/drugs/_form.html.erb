<%= form_with(model: drug) do |form| %>
  <% if drug.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(drug.errors.count, "error") %> prohibited this drug from being saved:</h2>

      <ul>
        <% drug.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :product_name, style: "display: block" %>
    <%= form.text_field :product_name %>
  </div>

  <div>
    <%= form.label :nmc, style: "display: block" %>
    <%= form.text_field :nmc %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
